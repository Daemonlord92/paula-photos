<%= form_with(model: photo_blog, local: true) do |form| %>
  <% if photo_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo_blog.errors.count, "error") %> prohibited this photo_blog from being saved:</h2>

      <ul>
      <% photo_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :photo_blog_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :photo_blog_description %>
  </div>

  <div class="field">
    <%= form.label :main_image %>
    <%= form.text_area :main_image, id: :photo_blog_main_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
